# 复制为 scripts/mimo.local.env（已在 .gitignore），课堂演示前加载：
#   Get-Content scripts/mimo.local.env | ForEach-Object { if ($_ -match '^([^#=]+)=(.*)$') { Set-Item -Path "env:$($matches[1])" -Value $matches[2] } }
LLM_PROVIDER=mimo
MIMO_API_KEY=your-key-here
MIMO_MODEL=mimo-v2.5
MIMO_BASE_URL=https://token-plan-cn.xiaomimimo.com/v1
